/**
 * The Notifications tab.
 *
 * The screen itself is components/notifications/NotificationsScreen — the same
 * one behind /modules/alerts. The two were near-identical copies of a mailbox
 * before, and drifted: one had an "All" tab the other did not, and neither had
 * the archive, the filters or the bulk actions.
 */
export { default } from '@/components/notifications/NotificationsScreen';

/**
 * Alerts — reached from the dashboard tiles and from Profile.
 *
 * Same mailbox as the Notifications tab, so it is the same screen rather than a
 * second copy to keep in step. See components/notifications/NotificationsScreen.
 */
export { default } from '@/components/notifications/NotificationsScreen';
